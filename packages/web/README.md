# BoardBack — Web App

The Next.js canvas app for BoardBack. See the [root README](../../README.md) for full project docs.

## Dev

```bash
npm run web:dev   # from repo root
```

Opens at [http://localhost:3000](http://localhost:3000).

## Build

```bash
npm run web:build
```
